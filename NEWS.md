# altdown 0.0.0.9000

* Initial release.
* Added `use_altdown()`, which copies the `altdoc/` template into a package
  and writes a starter `reference.yml`, instead of asking users to copy
  `altdoc/` in by hand.
